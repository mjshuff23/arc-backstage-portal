diff --git a/lib/elliptic/ec/index.js b/lib/elliptic/ec/index.js
index a3fe37e3a5f33f1519dbd605c9c8a47a14155deb..8722a2b42b62aac73d64daf7552b675da7734e82 100644
--- a/lib/elliptic/ec/index.js
+++ b/lib/elliptic/ec/index.js
@@ -153,6 +153,9 @@ EC.prototype.sign = function sign(msg, key, enc, options) {
   for (var iter = 0; ; iter++) {
-    var k = options.k ?
-      options.k(iter) :
-      new BN(drbg.generate(this.n.byteLength()));
-    k = this._truncateToN(k, true);
+    var k;
+    if (options.k) {
+      k = this._truncateToN(options.k(iter), true);
+    } else {
+      var kBytes = drbg.generate(bytes);
+      k = this._truncateToN(new BN(kBytes), true, kBytes.length * 8);
+    }
     if (k.cmpn(1) <= 0 || k.cmp(ns1) >= 0)
