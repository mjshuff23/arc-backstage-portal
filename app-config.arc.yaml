# Optional local ARC catalog ingestion.
#
# Backstage resolves file locations relative to the backend process working
# directory, which is normally packages/backend during local development.
catalog:
  locations:
    - type: file
      target: ../../../agents-with-remote-control-mobile-controller/catalog-info.yaml
      rules:
        - allow: [Component, API, Location, Group, Domain, System, Resource]
